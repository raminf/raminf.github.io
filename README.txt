To run locally:

> bundle install

Then: 

> bundle exec jekyll serve

and go to URL http://localhost:4000 to view.

To generate new PDF resume:

Open Chrome. Go to http://localhost:4000/resume

Click on 'Print' button, then save as PDF to the assets directory under 
Resume.pdf.

Push everything out to github pages.
